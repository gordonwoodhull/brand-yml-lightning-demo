---
title: brand.yml demo
format: html
---

Here's a block of code:
```{python}
def fib(n):
  if n<2: return n
  else: return fib(n-1) + fib(n-2)
```

And the result of `fib(17)`{.python} is `{python} fib(17)`.

Here is a [spiral](https://en.wikipedia.org/wiki/Spiral) rendered in plotnine:

```{python}
#| echo: false
#| tags: [parameters]

profile = 'none'
```


```{python}
#| echo: false
from quarto import theme_brand_plotnine

light_theme = theme_brand_plotnine(f'{profile}/brand-light.yml')
dark_theme = theme_brand_plotnine(f'{profile}/brand-dark.yml')
```

```{python}
#| echo: false
{{< include plotnine-renderings.py >}}
```
